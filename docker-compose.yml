services:
  voter-dost:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    container_name: voter-dost-pro
    restart: unless-stopped
